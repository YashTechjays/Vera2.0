steps:
  - name: gcr.io/cloud-builders/docker
    id: build
    args:
      - build
      - -f
      - vera-backend/docker/control_plane.Dockerfile
      - -t
      - us-central1-docker.pkg.dev/$PROJECT_ID/vera-repo/control-plane:latest
      - --cache-from
      - us-central1-docker.pkg.dev/$PROJECT_ID/vera-repo/control-plane:latest
      - vera-backend

  - name: gcr.io/cloud-builders/docker
    id: push
    args:
      - push
      - us-central1-docker.pkg.dev/$PROJECT_ID/vera-repo/control-plane:latest
    waitFor: [build]

options:
  logging: LEGACY
