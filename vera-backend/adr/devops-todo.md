# DevOps TODO — infrastructure obligations the app code cannot enforce

Some HIPAA / security guarantees live in the deployment, not in the codebase. When
a change in this repo *depends on* an infra property — a clock being synced, a key
being CMEK-backed, a role existing — record it here so it is tracked and verified,
not assumed. Each item names *what*, *why it matters*, and *where the dependency
came from*.

Status legend: ☐ open · ◐ in progress · ☑ done (link the PR / runbook / ticket).

| # | Item | Why it matters | Source |
|---|------|----------------|--------|
| 1 | ☐ **Verify the Cloud SQL Postgres host clock is NTP-synced**, and confirm the instance is *managed* Cloud SQL (not self-managed Postgres on a VM). Add clock-drift monitoring/alerting where available. | Postgres `now()` is the **single clock of record** for every `created_at`/`updated_at` and every audit + elevation timestamp (`audit_log`, `auth_audit_log`, `tenant_elevation`). HIPAA audit-trail integrity (§164.312(b)) depends on that clock being accurate and monotonic. Managed Cloud SQL hosts are NTP-synced by Google; a self-managed host is not, unless configured. | Timestamp audit (2026-06-17); reinforced by the 0004 migration that moves elevation expiry computation onto the DB clock. |
| 2 | ☐ **Provision a Cloud KMS key ring and symmetric encryption key for MFA envelope encryption**, grant the GKE workload-identity service account `roles/cloudkms.cryptoKeyEncrypterDecrypter` on the specific key, and set `VERA_KMS_KEY_NAME` (full resource path: `projects/{project}/locations/{location}/keyRings/{ring}/cryptoKeys/{key}`) in the GKE deployment env. Without this, `build_kms` falls back to `LocalDevKMS` and startup will fail with a `ValueError` (`LOCAL_KMS_MASTER_KEY` not set). | MFA TOTP seeds are envelope-encrypted at rest: the per-row DEK is wrapped by Cloud KMS (`GCPCloudKMS`) into `user_identity.totp_dek_ct`, and the seed ciphertext lives in `user_identity.totp_seed_ct`. Key rotation is forward-safe: `totp_key_ref` stores the version that wrapped each row's DEK; Cloud KMS `decrypt` selects the correct version automatically. | MFA DB envelope encryption (2026-06-19). |
| 3 | ☐ **Provision a LiveKit server or cluster (prod); store `LIVEKIT_URL`, `LIVEKIT_API_KEY`, and `LIVEKIT_API_SECRET` as secrets** (Secret Manager or equivalent — never plaintext deployment config). Inject them into both the control-plane deployment (`VERA_LIVEKIT_URL` is the URL the control plane uses to mint join tokens and dispatch the `vera-agent` worker) and the agent-worker deployment (all three vars, consumed by the LiveKit SDK). In dev, the docker-compose service uses `--dev` defaults (`devkey`/`secret` at `ws://localhost:7880`). | The control plane mints per-call join tokens and dispatches the `vera-agent` worker via these credentials; the worker connects to the same room. Leaking the API secret allows an attacker to create arbitrary rooms, join calls, and eavesdrop — keys must be rotated without downtime and must never appear in logs or source. | Voice pipeline core cascade (2026-06-19). |
| 4 | ☐ **Provision a Deepgram API key (prod); store it as a secret** (Secret Manager or equivalent) and inject as `DEEPGRAM_API_KEY` into the agent-worker deployment. | The agent worker passes every audio frame to Deepgram for STT. The key is per-project and has billing and usage-quota implications; exposure allows unbounded transcription charges. Because call audio transits Deepgram under the BAA, the correct Deepgram project and BAA attachment must be confirmed before any real-PHI calls are routed. | Voice pipeline core cascade (2026-06-19). |
| 5 | ☐ **Provision a Cartesia API key (prod); store it as a secret** and inject as `CARTESIA_API_KEY` into the agent-worker deployment. | The agent worker sends synthesised TTS text to Cartesia; the key is per-account with billing and quota implications. As with Deepgram, the Cartesia BAA attachment must be confirmed before real-PHI text is sent for synthesis. | Voice pipeline core cascade (2026-06-19). |
| 6 | ☐ **Configure Vertex AI / Gemini credentials for the agent worker (prod)**. Preferred path: bind a GKE Workload Identity service account with `roles/aiplatform.user` on the Vertex AI project. Fallback: inject `GOOGLE_APPLICATION_CREDENTIALS` (path to a service-account JSON mounted as a secret volume) or `GOOGLE_API_KEY` (for the `google` livekit-agents plugin's Gemini endpoint). | The LLM call is the reasoning core of every call turn; without valid credentials the worker fails at job dispatch. Vertex AI / Gemini falls inside the trust boundary (BAA-covered via Google Cloud healthcare terms); credentials must not be committed to source or appear in logs. | Voice pipeline core cascade (2026-06-19). |
| 7 | ☐ **Provision the GCS recording bucket (now wired).** CMEK-encrypted, uniform bucket-level access, **NO object versioning** (a noncurrent version would silently survive the retention sweeper's delete and falsify its `verified_gone` destruction audit; if versioning is ever forced on, add a lifecycle rule purging noncurrent versions immediately). Grant the LiveKit **egress** service account `roles/storage.objectCreator` on this bucket **only** (the composite-egress write path). Grant the **control-plane** SA read + delete scoped to this bucket only (`roles/storage.objectViewer` for sha256 verification + object delete for the retention sweeper — e.g. `roles/storage.objectUser` or a custom role, never bucket-wide admin). Set `VERA_RECORDING_BUCKET` (+ optional `VERA_RECORDING_PREFIX`) on the control-plane deployment; unset → recording is disabled end-to-end (no egress started, playback 409s). | The call audio is raw PHI at rest; CMEK is the storage-layer protection (application-level envelope encryption is deferred, per vera_core CLAUDE.md). Least-privilege split — egress writes, control-plane reads/deletes, nothing else — bounds the blast radius of either SA. | Call recording & persistence (2026-07-09); spec `docs/superpowers/specs/2026-07-09-call-recording-persistence-design.md`. |
| 8 | ☐ **SYNTHETIC / ROLE-PLAY DATA ONLY until the PHI codec is integrated**. The voice pipeline currently uses `PassthroughPHIBoundary` — no de-identification is applied between STT output and LLM input. Raw transcript text (including any identifiers present in audio) flows STT → LLM → TTS and into any transcript surface. No real patient, payer, or provider identifiers may be spoken or typed into a call session until `vera_core.phi.redact` is wired at the STT→LLM boundary and verified end-to-end. | There is no code-enforced de-identification in this pipeline slice; the passthrough seam is explicitly marked as a guardrail placeholder. Carrying real PHI through an unprotected pipeline violates the HIPAA de-identification requirement and Vera's own trust-boundary rules. This is a compliance guardrail, not a code check — human operators and QA must enforce it until the codec lands. | Voice pipeline core cascade (2026-06-19). |
| 9 | ◐ **Write an operator MFA-reset runbook (platform MFA stays TOTP-only).** Migration `f066c667ddc1` added the §C-style definer write path — but only for the *enrollment window* (`mfa_enabled=false`): browser-based first-login enrollment now works. Recovery codes were **deliberately not enabled** (consuming one needs a definer write on an *enrolled* row, breaking the enrollment-window guarantee), and `last_login_at` stamping remains open. A platform operator who loses their authenticator needs a documented manual reset (superuser: `UPDATE user_identity SET mfa_enabled=false, totp_seed_ct=NULL, totp_dek_ct=NULL, totp_key_ref=NULL, created_at=now() WHERE ...` → next login re-enrolls; the reset MUST bump `created_at` to reopen the time-boxed enrollment window — first login only issues a QR within `platform_enroll_window_seconds` of that timestamp). | The interim platform login (`/api/v1/platform/auth/*`, ADR-0006 §D-interim) runs as the RLS-bound app role, whose `platform_readable_rls_policy_ddl` WITH CHECK is strict equality — it **cannot UPDATE** a NULL-tenant `user_identity` row outside the sanctioned definer functions. TOTP-only is a deliberate security posture, not a gap, but it makes lockout recovery an *operational* procedure that must exist before production operators rely on it. | Interim platform password+MFA login (2026-06-21); browser MFA enrollment PR #68 (2026-07-09); ADR-0006 §D-interim. |
| 10 | ☐ **Harden the `auth_audit_log` hash chain from tamper-EVIDENT to tamper-PROOF.** The migration-0012 chain is a plain per-tenant SHA-256 chain: it detects after-the-fact edits, but a `BYPASSRLS` actor (the only role that can UPDATE/DELETE these WORM rows at all) can edit a row and recompute every later `row_hash`, leaving the chain self-consistent. Hardening options: (a) switch the row hash to an **HMAC keyed by a Cloud-KMS secret the DB role cannot read** (so a DB-level actor cannot forge the chain), and/or (b) **periodically anchor the head hash** to an append-only / object-locked external store (e.g. a WORM GCS bucket or transparency log) so any rewrite is externally detectable. Option (b) is implemented for the PHI `audit_log` (see #11); the same pattern remains open/TODO for `auth_audit_log`. | `auth_audit_log` is the authN/Z evidence trail (HIPAA §164.312(b)). Tamper-evidence catches accidental or unprivileged corruption, but a compromised privileged DB actor can silently rewrite history. Closing that gap requires a key the database cannot access or an external anchor — neither is built in this change. | auth_audit_log hash chain (2026-06-22). |
| 11 | ☐ **Provision an object-locked GCS audit-anchor bucket + least-privilege SA.** Create a bucket with a **locked retention policy** (irreversible; sized to the HIPAA minimum-retention requirement), **uniform bucket-level access**, **CMEK** encryption, and object versioning. Grant the control-plane CronJob's Workload Identity SA `roles/storage.objectCreator` on this bucket **only** (no delete/admin). Set `VERA_AUDIT_ANCHOR_BUCKET` (and optional `VERA_AUDIT_ANCHOR_PREFIX`) in the control-plane deployment; unset → `LocalFilesystemAnchorSink` (dev only). Schedule the `anchor-audit` entrypoint as a GKE CronJob (default hourly). | The audit_log hash chain (migration 0013) is tamper-EVIDENT only; anchoring each chain head to a WORM bucket makes a privileged in-DB rewrite externally detectable (closes devops-todo #10 option (b) for the PHI audit log). The bucket must be immutable and the job write-only so the anchor history cannot be edited or deleted. | PHI-audit WORM anchoring (2026-06-22); spec `docs/superpowers/specs/2026-06-22-phi-audit-worm-bucket-anchoring-design.md`. |
| 12 | ☐ **Grant the control-plane SA `roles/iam.serviceAccountTokenCreator` on ITSELF** (self-impersonation) so it can mint V4 signed playback URLs via IAM `signBlob` under Workload Identity. The playback endpoint (`GET /calls/{id}/recording`) signs time-boxed GCS URLs with `generate_signed_url(version="v4")` using the ambient SA's `signBlob` — no exported key file. Without this grant, signing raises at request time. | Signed URLs are how the browser fetches recording audio; minting them keylessly (signBlob) avoids a long-lived exported SA key (a standing PHI-access credential) at the cost of one self-token-creator grant. | Call recording & persistence (2026-07-09). |
| 13 | ☐ **Add a GCS bucket lifecycle rule as a retention BACKSTOP, sized behind the app-owned sweeper.** Set object auto-deletion at `max(tenant retention_days) + 30d` so the audited `RetentionSweeper` — which deletes at each tenant's `retention_until` and writes before/after `RECORDING_DELETED` audit rows — always acts first; the lifecycle rule only reaps objects the sweeper somehow missed. | The `audit_log` before/after deletion snapshots are the authoritative HIPAA destruction record. The lifecycle rule leaves no audit trail, so it must be a safety net that never fires ahead of the sweeper, not the primary deleter. | Call recording & persistence (2026-07-09). |
| 14 | ◐ **Lock down EXECUTE on the SECURITY DEFINER functions to the deployed app role.** Postgres grants EXECUTE to PUBLIC on new functions by default. The two platform-MFA functions (`f066c667ddc1`) are locked down by migration `3f7a9c2e8b41`: `REVOKE EXECUTE ... FROM PUBLIC` + `GRANT EXECUTE` to `$VERA_APP_DB_ROLE` (unset → `CURRENT_USER`). **Set `VERA_APP_DB_ROLE` to the deployed Cloud SQL app role in the prod/staging migration env**, else the grant lands on the migration runner, not the app role. Still open for the earlier definer functions (migrations `0002`, `0008`, `0012`, `0015`) **and now also the 3 platform-operator-lifecycle functions added by migration `d226261a20ca`** (`platform_create_operator_invite`, `platform_create_password_identity`, `platform_set_operator_status` — platform-user-invite feature, 2026-07-22), all of which continue to rely on the PUBLIC default plus their in-function guards; run the same `REVOKE`/`GRANT` for the deployed role there (tests use `vera_rls_test`). **Confirmed still live, not just theoretical** (2026-07-23): `deploy/remote-deploy.sh`'s only migration invocation — `docker compose run --rm -e VERA_DATABASE_URL="$migration_db_url" control-plane alembic upgrade head` — never sets `VERA_APP_DB_ROLE`, and no CI/deploy config anywhere sets it either (confirmed by grepping `.github/workflows/` and `deploy/`). So in the real `dev` (`vera-test`) environment today, every one of these 5 migrations' `GRANT EXECUTE` resolves to the migration role, not `vera_user` — meaning the deployed app cannot actually call any of these functions until this is fixed. Fix: add one new small Secret Manager value (e.g. `${SECRET_PREFIX}-app-db-role`, plaintext role name like `vera_user` — not sensitive, just a name) and pass it as `-e VERA_APP_DB_ROLE="$(fetch_secret app-db-role)"` alongside the `alembic upgrade head` line in `remote-deploy.sh`. | The definer functions are the privileged write paths (elevation grants, audit inserts, platform MFA enrollment, and now platform-operator invite/accept/deactivate). In-function guards hold, but role-scoped EXECUTE is the correct defense-in-depth: any *other* DB principal (reporting user, migration runner, future service) should not be able to invoke them at all — and today the deployed app role has NO execute grant on any of the 5 functions, so this is a live functional gap (`permission denied for function`), not only a defense-in-depth one. | Platform MFA enrollment security review, PR #68 (2026-07-09); re-confirmed + extended during platform-user-invite feature (2026-07-23), filed as GitHub issue #125. |
| 15 | ☐ **Provision an OpenAI API key (prod); store it as a secret** (Secret Manager with CMEK) and inject as `OPENAI_API_KEY` into the control-plane deployment. | The control plane uses OpenAI API as a fallback LLM tier for out-of-pipeline calls (via `vera_core.llm.ResilientLLM`): summaries, post-call processing, and extraction. The key is per-account with billing and usage-quota implications. Because calls made through OpenAI transit under the BAA, the OpenAI BAA attachment must be confirmed before any real-PHI data is sent. | Resilient LLM / call-summary feature (2026-07-17). |
| 16 | ☐ **Add an automated check that the deployed app's DB connection is genuinely non-superuser / non-BYPASSRLS.** Add a check (in `deploy/verify-app-env.sh`, or a new phase-2 deploy-time check run right after `render-env.sh`) that connects with the rendered `VERA_DATABASE_URL` and asserts `SELECT rolsuper, rolbypassrls FROM pg_roles WHERE rolname = current_user` returns `(false, false)`, failing the deploy otherwise. Today this correctness rests entirely on a human having followed the manual `CREATE ROLE vera_user ...` step in `deploy/README.md` §"Database (Cloud SQL) — one-time setup" when the Cloud SQL instance was first provisioned: `infra/terraform/modules/cloudsql` is still a pure skeleton (provisions nothing yet — see `main.tf`), and neither `verify-config.sh` nor `verify-app-env.sh` inspects the *privilege level* behind the `database-url` secret, only that the env var name is present and non-empty (confirmed by reading both scripts, 2026-07-23). | If the deployed app's DB role were ever accidentally superuser/BYPASSRLS (credentials mixed up with the migration role, or a fresh Cloud SQL instance stood up without following the runbook), every tenant/platform Row-Level-Security boundary in this app — the entire multi-tenant PHI isolation model — would silently stop being enforced, with **no code-level signal** anywhere. This is not hypothetical: reproduced locally during platform-user-invite manual verification (2026-07-23) — the local dev Postgres role is a superuser, and a tenant user visibly leaked into a platform-only operators list until the connection was swapped for a genuine non-superuser role, at which point isolation was correct again with no code change. | platform-user-invite feature manual verification (2026-07-23); GitHub issue #125. |
| 17 | ☐ **Provision the least-privilege `vera_user` Cloud SQL app role via infra-as-code, not a manual runbook step.** `deploy/README.md`'s one-time setup section already documents the *correct* design (`CREATE ROLE vera_user LOGIN PASSWORD '<strong-password>'` + scoped `GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES`, explicitly **not** superuser/BYPASSRLS, kept separate from the `postgres` migration role) — but it's a manual, human-run SQL step with no automation, idempotency check, or drift detection. When `infra/terraform/modules/cloudsql` (currently a skeleton — `main.tf` target-shape comment already says "app + migration DB roles (app role WITHOUT bypassrls)") is actually implemented, provision this role declaratively (Terraform `google_sql_user` + a bootstrap SQL script applied once per environment) so a new environment (staging, a rebuilt dev instance, a second region) can't silently skip this step the way a manual runbook can. | The entire least-privilege / RLS-enforcement design (ADR-0006 §3.5.9: "never `BYPASSRLS` on the app role") depends on this role existing correctly in *every* environment; today that correctness is one human following one runbook, with zero enforcement or verification. This item is the root-cause fix underneath #16 above — #16 is the automated safety-net *check*, this item fixes the *provisioning* so that check should never actually need to fire. | `infra/terraform/modules/cloudsql` skeleton note (`main.tf`); `deploy/README.md` §1; platform-user-invite feature investigation (2026-07-23). |
| 18 | ☐ **Configure Vertex AI / Gemini credentials for the CONTROL PLANE (prod), and set `VERA_GCP_PROJECT`** (+ optional `VERA_VERTEX_LOCATION`, default `us-central1`). Preferred: bind the control-plane GKE Workload Identity SA with `roles/aiplatform.user`. The post-call eval consumer only starts when `livekit_url`, `livekit`, AND `gcp_project` are all set; `VertexLLMClient` then calls Gemini Flash (`VERA_GEMINI_FLASH_MODEL`, default `gemini-2.5-flash`). Without creds the consumer boots+idles but every job routes to `EXCEPTION_REVIEW` on the LLM error. | The post-call re-read (Filled Form Eval) is the FIRST control-plane component to call an LLM — previously only the agent worker did (row 6). It reads only the already-de-identified transcript (no raw PHI to Gemini), but the call still transits Vertex under the BAA; confirm the correct Vertex project + BAA before real-PHI calls. | Filled Form Eval — post-call re-read (2026-07-09); spec `docs/superpowers/specs/2026-07-09-filled-form-eval-design.md`. |
| 19 | ☐ **DEFERRED — add a reaper/sweeper for forms stuck in `AI_PROCESSING`.** A form enters `AI_PROCESSING` in the call-end callback and only leaves when the post-call consumer processes its job. If the job is lost (consumer disabled, stream trimmed past `maxlen`, process down long enough), the form holds a VA concurrency slot forever with no auto-exit. Add a bounded sweep that routes `AI_PROCESSING` forms older than N minutes (DB-clock `updated_at < now() - interval`) to `EXCEPTION_REVIEW` (a legal edge) with an audit record — or at minimum a metric/alert on age. | Fixed-concurrency system: each stuck form is a slow-bleed slot leak that silently throttles a tenant's throughput. The two infinite-retry poison paths are already fixed (state guard + LLM-failure routing); this is the remaining "job never runs at all" gap. Deliberately deferred to keep this phase consumer-only (brainstorm Q2 chose consumer, not consumer+poller). | Filled Form Eval final review #1 (2026-07-09). |
| 20 | ☐ **COMPLIANCE DECISION — raw PHI to Vertex is deliberately NOT done.** The post-call pipeline consumes only the de-identified transcript as stored (tokenized at STT→LLM during the call), so raw identifiers never reach Gemini. Sending raw values to (BAA-covered) Vertex — e.g. to re-read identifier fields the de-identification obscured — would remove a defense-in-depth control and requires explicit BAA/compliance sign-off before implementation. | Records the design decision (spec D1) so a future "just send the real values, Vertex is BAA-covered" change goes through compliance, not a silent code edit. Default: keep the de-identified-only posture. | Filled Form Eval — post-call re-read (2026-07-09); spec D1. |
| 21 | ☐ **DEFERRED / COMPLIANCE-GATED — worker token-map seam for identifier reconciliation (Task 11).** Reconciling AI-restated identifiers (e.g. a member ID the payer reads back) against the intake baseline needs the worker to seed intake PHI at `open_session(known=…)` and stash a `field_path→token` map. But the agent worker is DB-less and does not receive intake PHI today; passing patient PHI to it is a new PHI-boundary decision. Until then, token-valued extracted fields safely route to `EXCEPTION_REVIEW`. `phi_codec` already exposes `vault.dump(session_id)` to enable the seam once unblocked. | Records why identifier reconciliation is not built and what it depends on, so it is a deliberate compliance-reviewed step later, not an oversight. | Filled Form Eval — post-call re-read (2026-07-09); spec D2. |

| 22 | ☐ **Leave `VERA_LOG_TOOL_REASONS` UNSET in staging and production.** The agent worker's six LLM tools (`end_call`, `task_complete`, `gap_complete`, `press_keypad`, `transfer_to_verification`, `give_up`) each take a model-authored `reason`. Unset (the default), `agent_worker/tool_log.py` logs only `tool <name>: reason len=N`; set to a truthy value it logs the sentence verbatim. It is a **dev / eval-run** switch. Add it to the deploy-time env checks (`deploy/verify-app-env.sh`) as a value that must be absent or falsey, since nothing in the code can tell which environment it is running in. | The reason is Gemini-authored prose describing live call state, so it can contain a member name, ID, or clinical detail. Logging it in production would put plaintext PHI in structured logs — a repo bright line (`vera-backend/CLAUDE.md`; `vera_core/CLAUDE.md`: transcript-derived text is unloggable, scrub to IDs/counts/shapes). The `phi_guardrails` PreToolUse hook cannot catch this: it matches PHI-ish *identifier names*, not arbitrary model text, so nothing enforces the default mechanically. | Tool-call reason logging (2026-08-01); decision recorded in-session — env-gated rather than always-on, precisely so prod keeps today's metadata-only behaviour. |

## How to use this file

- Add a row when a code change introduces (or surfaces) an infra dependency.
- Keep rows even after they're done (mark ☑ + link the evidence) — the audit trail
  of "we checked this" is itself useful for compliance review.
- This is an operational checklist, not an ADR; architectural *decisions* still go
  in a numbered ADR.
