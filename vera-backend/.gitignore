# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Environments
.venv/
.env
.env.*
!.env.example

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# OS / editor
.DS_Store
.idea/
.vscode/

# Terraform
infra/**/.terraform/
infra/**/*.tfstate*

# SDD controller scratch (briefs/reports/diffs/ledger)
.superpowers/

# Generated TTS probe audio (scripts/tts_probe.py)
tts-probe/

# Per-environment Langfuse files: contract rates (the app holds no prices, and a second
# copy in the repo is one that drifts) and, for anything but the local stack, that
# project's API keys. The glob deliberately spares *.example, which is the template.
langfuse-*.env
