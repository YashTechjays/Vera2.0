"""Vera agent worker — LiveKit Agents worker skeleton.

The worker registers itself with LiveKit under an agent_name and is dispatched
to rooms explicitly; the control plane never starts it. Built later this session.
"""
