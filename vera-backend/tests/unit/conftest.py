"""Shared fixtures for tests/unit/ (vera_core + control_plane).

`otel_spans`, `FakeTraceLinkRedis` and the tracer-provider `pytest_configure` moved up
to `tests/conftest.py` so the integration tree gets them too.
"""
